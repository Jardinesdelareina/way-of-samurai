import React from "react";
import s from "./Login.module.scss";

const Login = (props) => {
    return (
        <div className={s.login}>
            Login
        </div>
    )
}

export default Login;