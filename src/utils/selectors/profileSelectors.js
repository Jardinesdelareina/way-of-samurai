export const getProfile = (state) => {
    return state.profilePage.profile
}

export const getUserStatus = (state) => {
    return state.profilePage.status
}

export const getMyPost = (state) => {
    return state.profilePage.myPost
}